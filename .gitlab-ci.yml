before_script:
    - cd ./src
stages:
  - compile
  - cross-compile
compile:
  stage: compile
  script:
    - make host
cross-compile:
    stage: cross-compile
    script:
        - make target
after_script:
  #  - make clean