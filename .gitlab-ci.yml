before_script:
    - cd ./src/BusinessLayer/Logging
    - make lib-target
    - make lib-host
    - cd ../../API
    - make lib-target
    - make lib-host
    - cd ../
stages:
  - compile
  - cross-compile
compile:
  stage: compile
  script:
    - make host
cross-compile:
    stage: cross-compile
    script:
        - make target
after_script:
  #- make clean