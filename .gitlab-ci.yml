before_script:
    - cd ./src/BusinessLayer/Logging
    - make all
    - cd ../../API
    - make all
stages:
  - compile
  - cross-compile
compile:
  stage: compile
  script:
    - cd ./src
    - make host
cross-compile:
    stage: cross-compile
    script:
        - cd ./src
        - make target
after_script:
  #- make clean