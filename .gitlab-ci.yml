stages:
  - compile
  - cross-compile
compile:
  stage: compile
  script:
    - cd ./src
    - make
cross-compile:
    stage: cross-compile
    script:
        - cd ./src/API/
        - make

after_script:
  #- make clean